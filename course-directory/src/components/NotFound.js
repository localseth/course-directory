import React from 'react';
import ErrorDisplay from './ErrorDisplay';

const NotFound = () => {
    return(
        <ErrorDisplay message="Sorry, the page you are looking for could not be found" />
    )
}

export default NotFound;